# Gmail Subject Tagger

`GmailSubjectTagger` is the main application service responsible for coordinating Gmail and machine learning operations.

It combines:

- `GmailConnector`;
- `EmailCategorizer`;
- category configuration;
- confidence handling;
- label management.

:::src.services.tagger
